{
  "type": "module",
  "scripts": {
    "build": "npm run clean && node build.js",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "0.14.14",
    "esbuild-plugin-d.ts": "1.1.0",
    "rimraf": "3.0.2",
    "typescript": "4.5.5"
  }
}
